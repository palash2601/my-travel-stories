# my-travel-stories

ViaEuropa — curated European travel advisory app.

Deployment
- This project uses Vite and builds to the `dist` directory.
- Vercel configuration is included in `vercel.json` (rewrites SPA routes to `index.html`).

Local
- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
